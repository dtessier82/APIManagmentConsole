<UserControl x:Class="APIManagmentConsole.Views.Subscriptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="150">
    <Grid Background="WhiteSmoke">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions> 
        <Label Grid.Row="0" Content="SUSCRIPTIONS" FontWeight="ExtraBold" />
        <ListBox Grid.Row="1" BorderBrush="DarkRed" BorderThickness="3" FontWeight="Black"
     ItemsSource="{Binding SubscriptionsVM.Subscriptions}" DisplayMemberPath="DisplayName"
                 SelectedItem="{Binding SubscriptionsVM.SelectedSubscription}">
           
        </ListBox>
    </Grid>
</UserControl>
